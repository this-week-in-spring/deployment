apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: twi-studio
  name: twi-studio
spec:
  replicas: 1
  selector:
    matchLabels:
      app: twi-studio
  template:
    metadata:
      labels:
        app: twi-studio
    spec:
      containers:
        - envFrom:
            - configMapRef:
                name: twi-configmap
          image: gcr.io/bootiful/twi-studio:latest
          imagePullPolicy: Always
          name:  twi-studio
          readinessProbe:
            httpGet:
              port: 80
              path:  /
          livenessProbe:
            httpGet:
              port: 80
              path: /
          ports:
            - containerPort: 80

---
apiVersion: "v1"
kind: "Service"
metadata:
  name: twi-studio-service
  annotations:
    cloud.google.com/neg: '{"ingress": true}'
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: twi-studio
  type: LoadBalancer
---
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  annotations:
    ingress.kubernetes.io/force-ssl-redirect: "true"
    networking.gke.io/managed-certificates: "twi-studio-certificate"
    kubernetes.io/ingress.global-static-ip-name: "twis-twi-studio-ip"
  name: twi-studio-ingress
spec:
  rules:
    - host: studio.twis.online
      http:
        paths:
          - backend:
              serviceName: twi-studio-service
              servicePort: 80
---
apiVersion: networking.gke.io/v1beta2
kind: ManagedCertificate
metadata:
  name: twi-studio-certificate
spec:
  domains:
    - studio.twis.online